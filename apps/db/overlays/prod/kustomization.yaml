apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: tn-don-db-prod
namePrefix: don-db-

patches:
  - target:
      kind: Cluster
      name: postgres
    patch: |
      - op: replace
        path: /spec/instances
        value: 2
