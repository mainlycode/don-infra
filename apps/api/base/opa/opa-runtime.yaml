apiVersion: apps/v1
kind: Deployment
metadata:
  name: opa
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: opa
  template:
    metadata:
      labels:
        app.kubernetes.io/name: opa
    spec:
      containers:
        - name: opa
          image: openpolicyagent/opa:1.12.1
          args:
            - run
            - --server
            - --addr=0.0.0.0:8181
            - --log-level=debug
            - --ignore=.*
            - /policies
            - /data
          ports:
            - containerPort: 8181
              name: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - name: opa-policy
              mountPath: /policies
              readOnly: true
            - name: opa-data
              mountPath: /data
              readOnly: true
      volumes:
        - name: opa-policy
          configMap:
            name: opa-policy
        - name: opa-data
          secret:
            secretName: opa-auth-config
            optional: true
---
apiVersion: v1
kind: Service
metadata:
  name: opa
spec:
  selector:
    app.kubernetes.io/name: opa
  ports:
    - name: http
      port: 8181
      targetPort: http
