apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: register-site-http-route
spec:
  parentRefs:
    - name: public-gateway
      namespace: istio-gateway
      sectionName: https
  hostnames:
    - set-in-overlay
  rules:
    - backendRefs:
        - name: register-site-svc
          kind: Service
          port: 4321
